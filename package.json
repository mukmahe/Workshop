{
  "name": "automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "intractive": "npx cypress open",
    "headless": "npx cypress run --headless -b chrome",
    "google": "npx cypress run --headless -b chrome --spec './cypress/integration/tests/google.js'",
    "edge": "npx cypress run --headed -b edge",
    "chrome": "npx cypress run --headed -b chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^4.10.0"
  }
}
