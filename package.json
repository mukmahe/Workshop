{
  "name": "automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "requestTimeout": 10000,
  "componentFolder": "cypress/component",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "execute": "node  cypress/scripts/cypress.js",
    "intractive": "npx cypress open",
    "headless": "npx cypress run --headless -b chrome",
    "edge": "npx cypress run --headed -b edge",
    "chrome": "npx cypress run --headed -b chrome",
    "qa-env": "npx cypress run --env url='www.google.com' --headed -b chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^4.10.0",
    "cypress-file-upload": "^4.1.1",
    "mochawesome": "^6.1.1",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^5.1.0"
  }
}
